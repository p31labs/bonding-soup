# Nested remotes (their own .git)
andromeda/
phosphorus31.org/

# Dependencies & artifacts
node_modules/
**/node_modules/
simplex-v7/node_modules/
simplex-email/node_modules/
dist/
spikes/sovereign-geodesic-preview/dist/
.cache/
.npm/
wcd33-global-archive/node_modules/

# Home directory noise (if present under P31 home root)
.Xauthority
.bash_history
.bash_logout
.bashrc
.bun/
.claude.json
.claude/
.config/
.gh.json
.gitconfig
.pm2/
.profile
.sommelierrc
.sudo_as_admin_successful
.wrangler/
signal-*.gpg
signal-*.sources

# Local / secrets
.env
.env.*
*.local

# Operator shift log if kept under repo root (canonical: ~/.p31/ per AGENTS)
.p31/

# IDE / agent caches
.cursor/
.codex/
.kilo/
**/.kilo/
*.swp
*~
.viminfo

# Local security / browser state (never commit)
.pki/

# Local shell helpers (repo-root noise)
.fzf.bash

# Local Cursor installer unpack (very large; never commit)
cursor_*_amd64/

# Test artifacts
playwright/test-results/
playwright/report/
coverage/

# Stray local Playwright tree (e2e lives under andromeda/04_SOFTWARE/p31ca)
/playwright/

# Discovery workspace — court / discovery materials and local venv (never commit)
# Operator rule: do not `git add Discovery/` for court exhibits, statements, or handoff PDFs/DOCX.
Discovery/assembled_exhibits/
Discovery/.venv/
Discovery/__pycache__/
# Top-level PDFs (Discovery/**/*.pdf alone does NOT match Discovery/foo.pdf)
Discovery/*.pdf
Discovery/**/*.pdf
Discovery/*.docx
Discovery/**/*.docx
Discovery/DISCOVERY_RESPONSE_HANDOFF.md
Discovery/SUPPLEMENTAL_DISCOVERY_ASSEMBLY_PROMPT.md
Discovery/monthly-statement*.pdf
Discovery/Gmail*.pdf

# Scratch / generated reports
final_validation_report.md
audit_runner.py
docs/generated/

# Zenodo local scan outputs (generated by p31labs/scripts/zenodo_scan_local.py)
p31labs/out/
p31labs/zenodo_scan_*.json
p31labs/zenodo_scan_*.md
/zenodo_scan_*.json
/zenodo_scan_*.md

# Local installers / large binaries (not tracked)
/Applications/
*.AppImage
*.deb

# Logs
*.log
npm-debug.log*
