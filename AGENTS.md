# P31 home workspace — agent notes

This tree is a **multi-root** home: **BONDING Soup** at the top level, **Andromeda** under `andromeda/`, other sites, `docs/`, and tooling. The **P31 home** root may be its own git repository (e.g. Soup + `cognitive-passport` + `scripts` + `wcd33-global-archive/`; `.gitignore` excludes `andromeda/` and `phosphorus31.org/`, which have separate remotes). `andromeda/` remains the monorepo for `04_SOFTWARE` and GitHub Actions.

0. **New clone / machine:** **`npm run setup`** (root + `p31ca` `npm install`, `apply:constants`, `apply:p31-style` when design tokens exist, **`npm run verify`**, optional **`git config core.hooksPath .githooks`** via **`npm run git:hooks`** so **`.githooks/pre-commit`** runs **`verify:monetary`** when payment/creator-economy files are staged; bypass with **`P31_SKIP_MONETARY_HOOK=1`**). **`.githooks/post-commit`** can **auto-push** when **`npm run git:autopush:on`** or **`P31_AUTO_PUSH=1`**; off with **`P31_NO_AUTO_PUSH=1`**. **Clickable “command center”** (local only): **`npm run command-center`** opens **http://127.0.0.1:3131** with whitelisted run buttons (**`verify`**, **`verify:mesh`**, **`ecosystem:glass`**, **`operator:shift-status`**, monetary/MAP, **`release:public`**, git/PR), or use VS Code / Cursor **Tasks: Run Task** → **“P31: local command center (clickable buttons; Ctrl+C to stop server)”** (search **P31**). **`P31_CMD_CENTER_NO_OPEN=1`** disables auto-open browser. **Engineering standard (normative):** **`docs/P31-ENGINEERING-STANDARD.md`** — definition of done: **`npm run verify`**, **`npm run release:check`**, constants/canon/passport rules, secrets, **`docs/files/`** binary policy in Andromeda. **Full-fleet “everything connected” / 11/10:** **`docs/ECOSYSTEM-PRODUCTION-11.md`** (glass ladder, deploy order, hub + K₄ + operator + monetary matrix). Monorepo production bar: **`andromeda/docs/ENTERPRISE_QUALITY.md`**. **K₄ edge:** **`npm run verify:mesh`** = k4-personal wrangler dry-run + live **`/api/health`** + **`/api/mesh`** (defaults strict vs `p31-constants.json` `mesh.k4PersonalWorkerUrl`). **Git remotes / PR automation:** **`p31-github.json`** (home **`origin`** via **`homeRepository`**, default **`p31labs/bonding-soup`**; override **`P31_HOME_GITHUB`**) + **`npm run git:remotes`**. **DX / friction:** **`npm run doctor`** (Node, remotes, **`gh`**, Andromeda **`origin`**); **`npm run frictionless`** = doctor + **`release:local`** (same hub chain as **`release:all`** but **`MESH_LIVE_STRICT=0`**); **`npm run doctor -- --verify`** or **`--mesh`**. **Low-friction PR + auto-merge:** **`npm run pr`** / **`pnpm pr`** — repo from cwd; on **main**/**master**, **`gh-pr-automerge`** auto-creates **`pr/<slug>-<shortsha>`** unless **`P31_PR_NO_AUTO_BRANCH=1`**. Overrides: **`P31_PR_IN_HOME=1`**, **`P31_PR_IN_ANDROMEDA=1`**. **Credential / “gitci” noise:** **`pnpm run fix:gh`** (or **`npm run fix:gh`**) runs **`gh auth setup-git`**. Full flags still available: **`npm run gh:pr:automerge -- -- …`**. Requires **`gh`**, **Allow auto-merge**, feature branch.

**GitHub `p31-ci.yml`** (workflow **P31 CI**): two jobs — **`P31 / root verify`** then **`P31 / full stack`** (preflight avoids duplicate `npm run verify`). Sets **`MESH_LIVE_STRICT=1`** on the full pass. On **`push`** and **`pull_request`** to **`main`/`master`**. **Branch protection:** require both check names. Local **`p31:ci` / `release:check`** uses **loose** live mesh (skip with **`MESH_LIVE_STRICT=0`**) so offline runs pass; use **`MESH_LIVE_STRICT=1 npm run p31:ci`** to match the mesh parts of the full job. Task: **P31: verify mesh (k4-personal dry-run + live /api/*)** in **`.vscode/tasks.json`**.

1. **Mesh / K₄ numbers in docs or chat:** do not invent from prose (e.g. WCD docs alone). Use **live service bindings** when available, or **`p31.ground-truth`**, or **`p31ca` `scripts/hub/registry.mjs` / generated JSON** for product URLs and counts — and treat anything else as unverified.
2. **Start with** **`P31-ROOT-MAP.md`**, or the shorter cheat sheet **`docs/P31-PERSONAL-HOW-TO.md`**, when you need “which command / which repo.” **P31-ROOT-MAP (detail):** which directory to change for Soup vs monorepo vs static hub. **p31ca ground truth (contract):** `andromeda/04_SOFTWARE/p31ca/ground-truth/p31.ground-truth.json` — edge redirects, key registry `appUrl` invariants, and Three.js pins. **Synergetic multi-dome map:** `p31ca/ground-truth/synergetic-manifest.json` — surfaces (Astro dome, static observatory, Spaceship Earth PWA); **`npm run verify:synergetic`** in p31ca (part of **`prebuild`**). Product epics: **`docs/WORK-PACKAGE-SYNERGETIC-GEODESIC-STACK.md`**. **GEODESIC static page — coach, unlocks, K4 live, events:** **`docs/GEODESIC-CAMPAIGN.md`**. **Operator-locked numbers (EIN, BONDING 424/32, Larmor Hz, etc.):** edit **`p31-constants.json`**, then **`npm run apply:constants`** and **`npm run verify:constants`**. **Universal UI canon (colors, typography, spacing, motion, hub vs org appearance):** **`andromeda/04_SOFTWARE/design-tokens/p31-universal-canon.json`** — **`npm run apply:p31-style`** (regenerates hub **`public/p31-style.css`** + **`p31-tailwind-extend.js`**, syncs Tailwind CDN static pages, mirrors **`cognitive-passport/p31-style.css`**); org/light for phosphorus31.org via **`data-p31-appearance="org"`** (see **`design-tokens/PHOSPHORUS31-RING.md`**). **`npm run verify:p31-style`** is part of root **`npm run verify`** and p31ca **`prebuild`**. **`npm run verify:ground-truth`** in p31ca (also in **`prebuild`** / **`hub:ci`**). Root: **`npm run verify`** = passport + constants + **`verify:p31-style`** + **`verify:p31ca-contracts`** (ground-truth + synergetic + **Lattice Oracle** + **`verify:economy`** when `p31ca` exists; skips partial clone) + **`verify:egg-hunt` / `verify:quantum-egg`** (off-path routes + Larmor display vs `p31-constants.json` — **`docs/EGG-HUNT.md`**) + `tsc`; **`npm run verify:all` / `p31:ci`** = full p31ca build chain. **Version *meaning*:** **`docs/CANONICAL-NUMBERING.md`**. CogPass **edition** = H1 in the markdown file (e.g. 5.1), not the JSON schema or filename alone.

   - **K₄ + agentic (short normative maps):** **`docs/SIC-POVM-K4-ARCHITECTURE.md`** (four-vector: physical / network / compliance / UX; metaphor + edge patterns); **`docs/AGENTIC-VIBE-INFRASTRUCTURE.md`** (vibe / agentic dev tied to `verify` and rules); **`docs/PLAN-KIDS-VIBE-CODING.md`** (household youth, local-first). Cross-refs **`docs/EGG-HUNT.md`**, **`docs/SOULSAFE-TETRA-SPEC.md`**.
3. **Operator and mesh context:** **`CLAUDE.md`** and **`.cursorrules`** (K₄, spoons, S.J./W.J., no naval metaphors, legal/ops).
4. **Cognitive Passport:** authoring **`cognitive-passport/index.html`**; deploy mirror **`andromeda/04_SOFTWARE/p31ca/public/passport-generator.html`**. Live short URL: **`https://p31ca.org/passport`**. After edits: **`npm run sync:passport`**, then **`npm run verify:passport`** (skips if the p31ca path is missing — partial clone / no Andromeda). See **`.cursor/rules/cognitive-passport-mirror.mdc`**. Transform (canonical): **`andromeda/04_SOFTWARE/p31ca/scripts/passport-p31ca-transform.mjs`**; root re-export: **`scripts/passport-p31ca-transform.mjs`**. Do not hand-edit the mirror to diverge.
5. **Andromeda** has its own workflows; see **`andromeda/CONTRIBUTING.md`**, **`andromeda/docs/REPOSITORY_LAYOUT.md`**, and **`andromeda/04_SOFTWARE/README.md`** (Cognitive Passport subsection for cross-workspace).
6. **Run Task** (Cursor/VS Code): **P31: sync passport → p31ca** (`.vscode/tasks.json`), **P31: verify monetary (ecosystem + constants + economy mirror)**, **P31: git remotes (origin + andromeda)**, **P31: release:check** (**`node scripts/p31-ci.mjs`**: root `npm run verify` + p31ca build **only if** `andromeda/04_SOFTWARE/p31ca` exists; home-only clone passes without hub build), **P31: release:public** (**`npm run release:public`** — **`verify`** + strict mesh + **`hub:ci`** + **security:check**; flags **`--content`**, **`--no-security`**), **P31: release:all** (strict mesh + hub **`verify`** + p31ca **security:check**; no about-page regen), **P31: p31:all (everything)** (`scripts/p31-all.mjs`: same as **release:all** + **`validate:full`**, **fleet:probe** soft, **ecosystem glass** soft — live probes from **`p31-ecosystem.json`** → `/tmp/p31_glass_report.json`; **`--skip-ecosystem-glass`** to omit), **Playwright e2e**, **security:lint** soft, **Semgrep** soft if CLI installed; **`.github` `p31-ci.yml`** uses **`p31:all`**), **P31: wcd33 worker tail**, **P31: Cloudflare wrangler inventory**, **P31: p31ca hub:ci** (registry → about + verify + build). Root: **`npm run release:check`**, **`npm run release:public`**, **`npm run release:all`**, **`npm run p31:all`**, **`npm run validate:full`** (extended shell validation + quantum egg + live audits), **`npm run inventory:cf`**, **`npm run ecosystem:glass`** (glass box only), **`npm run ecosystem:plan`** / **`ecosystem:deploy:dry`** (ordered deploy list; execute only with **`P31_ECOSYSTEM_DEPLOY=I_UNDERSTAND npm run ecosystem:deploy`**), **`npm run operator:shift-in`** / **`operator:shift-out`** (local **`~/.p31/operator-shift.jsonl`** audit for on-call focus). **k4-personal** (personal K₄ Worker): `andromeda/04_SOFTWARE/k4-personal` is a **pnpm** workspace package — from `04_SOFTWARE` run **`pnpm install`**, then **`pnpm --filter k4-personal verify`** (dry-run) or **`pnpm --filter k4-personal deploy`**; root **`npm run verify:k4-personal`** / **`release:check`** run the same bundle check. See **`k4-personal/README.md`**. **Node 20** — repo root **`.nvmrc`**. Andromeda **P31 Automation** manual deploy: optional **`p31ca_strict_passport`** requires `cognitive-passport` in the checkout (or fails).

7. **Parallel agents (Andromeda):** if another session is editing **`p31ca`**, read **`andromeda/04_SOFTWARE/p31ca/docs/PARALLEL_AGENT_COORDINATION.md`** before large `index.astro` or ground-truth edits.

8. **Site updates in workflow (velocity):** treat ship cadence as **default**, not an afterthought. **Technical hub (p31ca.org):** lives under **`andromeda/04_SOFTWARE/p31ca/`** — after registry or content changes, **`npm run hub:ci`** (runs full **`verify`** + **`postbuild` `verify-p31ca-dist`** so `dist/` is never shipped incomplete), then passport sync if the generator changed. **One-command deploy (home, Andromeda present):** **`npm run deploy:p31ca`** = `verify` + `wrangler pages deploy dist` (needs `CLOUDFLARE_API_TOKEN`); else deploy per **`p31ca/DEPLOY.md`**. **CI:** `p31ca-hub.yml` (push to `main` auto-deploys) + `p31-automation` / Pages jobs in Andromeda. **Parallel path — phosphorus31.org:** a **separate** tree, package graph, and deploy story; another agent or operator may own it. **Do not** mix p31ca and `phosphorus31.org/` changes in the same PR unless the operator explicitly wants one coordinated release. See **`P31-ROOT-MAP.md`** (site update tracks).

8b. **Operator UI CWP (interactive shell, glass box, tag in/out):** canonical **`andromeda/04_SOFTWARE/p31ca/docs/CONTROLLED-WORK-PACKAGE-INTERACTIVE-OPERATOR-UI.md`** (`CWP-P31-UI-2026-01`); home pointer **`docs/CWP-POINTER-INTERACTIVE-OPERATOR-UI.md`**; public route when deployed **`/ops/`** on p31ca. Glass probe list: home **`p31-ecosystem.json`** → p31ca **`npm run ops:ingest`** → **`src/data/ops-glass-probes.json`** (prebuild ingests when home tree present). Sister to ECO (`CWP-P31-ECO-2026-01`); no Worker secrets in static HTML.  

8c. **Personal Agent Room (PAR) CWP:** **`andromeda/04_SOFTWARE/integration-handoff/CONTROLLED-WORK-PACKAGE-PERSONAL-AGENT-ROOM.md`** (`CWP-P31-PAR-2026-01`); handoff **`andromeda/04_SOFTWARE/integration-handoff/CWP-31/`**; product mesh map **`docs/MESH-MAP-PERSONAL-START-PAGES.md`**. **BONDING Soup** **`soup-demo.html`** (same **`npm run demo`** as passport) links to **`p31ca/public/planetary-onboard.html`** and **`mesh-start.html`** for dev; `verify:mesh` exercises k4-personal vs constants.  

8d. **Initial Build (IB) CWP (production):** **`andromeda/04_SOFTWARE/integration-handoff/CONTROLLED-WORK-PACKAGE-INITIAL-BUILD.md`** (`CWP-P31-IB-2026-01`); handoff **`andromeda/04_SOFTWARE/integration-handoff/CWP-32/`**; normative spec **`andromeda/04_SOFTWARE/integration-handoff/INITIAL-BUILD-SITE-STRICT-PLAN.md`**. **Live** **`https://p31ca.org/build`** → `initial-build.html` (ground-truth + `_redirects`). **Sister** to PAR; full intake/PUT WBS in CWP.  

9. **Handoff for Gemini / Opus (or other model review):** **`docs/README-REVIEW-DOCS.md`** — bundle + CI / worker / CWP supplements + **`docs/MVP-DELIVERABLES-INVENTORY.md`** (tiered MVP / grant bullets). **Command center (EPCP Worker + p31ca Orchestrator / external orchestrator):** step-by-step paste prompt in **`docs/HANDOFF-PROMPT-COMMAND-CENTER.md`**. Root **`npm run p31:ci`** = `scripts/p31-ci.mjs` (**`npm run verify`** then p31ca verify+build; in `CI` also root + p31ca **`npm ci`**; **`p31ca` security suite** after hub build). **`npm run p31:ci:all` / `release:all`**: strict mesh + same chain + **`--security`**. **`npm run p31:all`**: that plus **`validate:full`**, e2e, fleet, lint, Semgrep (see `scripts/p31-all.mjs`). **Updated 2026-04-25.** The Triad includes **KwaiPilot** (Node Zero FW execution). WCD Batch 3 (WCDs 26–32) closed; CogPass long-form **edition 5.1** (`CANONICAL-NUMBERING`); P31 Labs, Inc. incorporated Apr 3, 2026; EIN 42-1888158; Stripe Checkout via **`donate-api.phosphorus31.org`** Worker (HCB unresponsive; no separate `api.phosphorus31.org` until deployed). See review bundle **§12** for snapshot.

10. **Public surfaces shipped 2026-04-25 (p31ca public release batch):** Four new live pages in `andromeda/04_SOFTWARE/p31ca/public/` — **`planetary-onboard.html`** (five-phase neuro-inclusive onboarding; Void → Anchor → Rooms → Dial → Pact; WebAuthn Phase 5 with Worker fallback stub; archetype routing `?a=child|elder`); **`connect.html`** (Three.js r160 K₄ cage navigator — family tetrahedron + 8 product satellites, EBC footer); **`delta.html`** (Tailwind CDN narrative page — Wye→Delta resilience story, comparison table, CTAs to onboarding and mesh); **`tomography.html`** (Three.js r183 depth companion to k4market — symbol-seeded Larmor ring). **Super-Centaur operator pack:** **`p31-super-centaur-starter.html`** + **`p31-super-centaur-pack.json`**; short redirects **`/centaur`** and **`/super-centaur`**; hub registry id **`super-centaur`**; **`npm run verify:super-centaur-pack`** (p31ca `prebuild`) checks pack shape and **`meshFleet` ↔ `integration-handoff/CWP-30/mesh-bridge.ts`**. **Passkey Worker:** `andromeda/04_SOFTWARE/p31ca/workers/passkey/` — KV `CHALLENGES` (5-min TTL), D1 `p31-passkey-db`, POST `register-begin/finish`, `auth-begin/finish`. **Production mount:** zone route `p31ca.org/api/passkey/*` (see `workers/passkey/wrangler.toml`); hub static pages use **same-origin** `fetch('/api/passkey/…')`. Canonical path: **`p31-constants.json`** `mesh.passkeyApiBasePath` → `src/data/p31-mesh-constants.json` via **`npm run apply:constants`**. Ground-truth route **`passkeyApi`**. Deploy recipe: `workers/passkey/README.md`. RP_ID: `p31ca.org` (prod), `p31ca.pages.dev` (preview). Three.js pins in `threejs` section for surfaces that use it. About pages auto-generated via `generate-about-pages.mjs`.

11. **Security suite (p31ca):** `npm run security:check` — SCA (npm audit + suppressions), worker inventory + CORS scan, PQC test gate. **`npm run security:check:full`** runs Phase A (p31ca `verify`) then B–E; `p31-ci.mjs` already runs `verify`, so the automated stack uses **`security:check`** (skip-A) after the hub build. Policy: P0 = unsuppressed prod-runtime vulns or broken quantum-core tests (CI fails); P1 = dev-dep CVEs + worker gaps (warns, CI passes). Runbook: `andromeda/04_SOFTWARE/p31ca/docs/SECURITY-RUNBOOK.md`. Edge trust boundary: `andromeda/04_SOFTWARE/p31ca/docs/EDGE-SECURITY.md` (from home root; not under top-level `docs/`). Suppressions: `security/audit-suppressions.json`. Worker allowlist: `security/worker-allowlist.json`. **Home `p31-ci.yml`** runs **`security:check`** when `p31ca` is in the checkout. Weekly Semgrep: `.github/workflows/p31-security.yml`. When adding a worker: run `security:workers` and add to allowlist before merging.

12. **Creator economy contract (p31ca):** `ground-truth/creator-economy.json` — `p31.creatorEconomy/1.0.0` schema. Verifier: `npm run verify:economy` (in prebuild; fails CI if fees change without a commit). Public endpoint: `https://p31ca.org/creator-economy.json` (CORS `*`). Version 1.0.0 constraints enforced by CI: platform fee = 0, creator share = 1.0, room access free. To change: edit the JSON, update both `ground-truth/creator-economy.json` and `public/creator-economy.json`, run verifier, commit. 30-day notice policy documented in the contract.

13. **Personal start pages + agents (vision + phases):** **`docs/MESH-MAP-PERSONAL-START-PAGES.md`** — per-member landing + `k4-personal` Durable Object agent (`/agent/:userId/...`); ties to planetary onboard archetypes and cage isolation. **SOULSAFE tetra (four-effect fusion on `/chat`):** **`docs/SOULSAFE-TETRA-SPEC.md`**.

14. **Geodesic campaign contract:** `andromeda/04_SOFTWARE/p31ca/ground-truth/geodesic-campaign.json` — canonical 5-track coach data (Explorer→Scootch→Sticky→Builder→Mesh). TypeScript types + export: `andromeda/04_SOFTWARE/packages/shared/src/geodesic-campaign.ts` (importable as `@p31/shared/geodesic-campaign`). Verifier: `npm run verify:geodesic-campaign` (in p31ca `prebuild`). To change campaign: edit `geodesic-campaign.json` + mirror into the `const CAMPAIGN = { ... }` block in `p31ca/public/geodesic.html` + mirror into `packages/shared/src/geodesic-campaign.ts`. **GeodesicRoom DO v0.2.0** (Phase 1): supports ADD_SHAPE/MOVE_SHAPE/REMOVE_SHAPE/RESET_SHAPES + hello.shapes; 50-shape cap; Maxwell rigidity; worker: `geodesic-room.trimtab-signal.workers.dev`. **Non-browser clients:** **`docs/GEODESIC-GAME-ENGINE-INTEGRATION.md`** (Wire: `p31.geodesicRoomWire/0.2.0` — `geodesic-room/src/index.ts`); **TS types:** `@p31/shared/geodesic-room-wire` — must stay in sync with the Worker.
