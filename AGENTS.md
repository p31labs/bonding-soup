# P31 home workspace — agent notes

This tree is a **multi-root** home: **BONDING Soup** at the top level, **Andromeda** under `andromeda/`, other sites, `docs/`, and tooling. The **P31 home** root may be its own git repository (e.g. Soup + `cognitive-passport` + `scripts` + `wcd33-global-archive/`; `.gitignore` excludes `andromeda/` and `phosphorus31.org/`, which have separate remotes). `andromeda/` remains the monorepo for `04_SOFTWARE` and GitHub Actions.

1. **Mesh / K₄ numbers in docs or chat:** do not invent from prose (e.g. WCD docs alone). Use **live service bindings** when available, or **`p31.ground-truth`**, or **`p31ca` `scripts/hub/registry.mjs` / generated JSON** for product URLs and counts — and treat anything else as unverified.
2. **Start with** **`P31-ROOT-MAP.md`** — which directory to change for Soup vs monorepo vs static hub. **p31ca ground truth (contract):** `andromeda/04_SOFTWARE/p31ca/ground-truth/p31.ground-truth.json` — edge redirects, key registry `appUrl` invariants, and Three.js pins. **Synergetic multi-dome map:** `p31ca/ground-truth/synergetic-manifest.json` — surfaces (Astro dome, static observatory, Spaceship Earth PWA); **`npm run verify:synergetic`** in p31ca (part of **`prebuild`**). Product epics: **`docs/WORK-PACKAGE-SYNERGETIC-GEODESIC-STACK.md`**. **GEODESIC static page — coach, unlocks, K4 live, events:** **`docs/GEODESIC-CAMPAIGN.md`**. **Operator-locked numbers (EIN, BONDING 413/30, Larmor Hz, etc.):** edit **`p31-constants.json`**, then **`npm run apply:constants`** and **`npm run verify:constants`**. **Universal UI canon (colors, typography, spacing, motion, hub vs org appearance):** **`andromeda/04_SOFTWARE/design-tokens/p31-universal-canon.json`** — **`npm run apply:p31-style`** (regenerates hub **`public/p31-style.css`** + **`p31-tailwind-extend.js`**, syncs Tailwind CDN static pages, mirrors **`cognitive-passport/p31-style.css`**); org/light for phosphorus31.org via **`data-p31-appearance="org"`** (see **`design-tokens/PHOSPHORUS31-RING.md`**). **`npm run verify:p31-style`** is part of root **`npm run verify`** and p31ca **`prebuild`**. **`npm run verify:ground-truth`** in p31ca (also in **`prebuild`** / **`hub:ci`**). Root: **`npm run verify`** = passport + constants + **`verify:p31-style`** + **`verify:p31ca-contracts`** (ground-truth + synergetic + **Lattice Oracle** when `p31ca` exists; skips partial clone) + **`verify:egg-hunt` / `verify:quantum-egg`** (off-path routes + Larmor display vs `p31-constants.json` — **`docs/EGG-HUNT.md`**) + `tsc`; **`npm run verify:all` / `p31:ci`** = full p31ca build chain. **Version *meaning*:** **`docs/CANONICAL-NUMBERING.md`**. CogPass **edition** = H1 in the markdown file (e.g. 5.1), not the JSON schema or filename alone.
3. **Operator and mesh context:** **`CLAUDE.md`** and **`.cursorrules`** (K₄, spoons, S.J./W.J., no naval metaphors, legal/ops).
4. **Cognitive Passport:** authoring **`cognitive-passport/index.html`**; deploy mirror **`andromeda/04_SOFTWARE/p31ca/public/passport-generator.html`**. Live short URL: **`https://p31ca.org/passport`**. After edits: **`npm run sync:passport`**, then **`npm run verify:passport`** (skips if the p31ca path is missing — partial clone / no Andromeda). See **`.cursor/rules/cognitive-passport-mirror.mdc`**. Transform (canonical): **`andromeda/04_SOFTWARE/p31ca/scripts/passport-p31ca-transform.mjs`**; root re-export: **`scripts/passport-p31ca-transform.mjs`**. Do not hand-edit the mirror to diverge.
5. **Andromeda** has its own workflows; see **`andromeda/CONTRIBUTING.md`**, **`andromeda/docs/REPOSITORY_LAYOUT.md`**, and **`andromeda/04_SOFTWARE/README.md`** (Cognitive Passport subsection for cross-workspace).
6. **Run Task** (Cursor/VS Code): **P31: sync passport → p31ca** (`.vscode/tasks.json`), **P31: release:check** (**`node scripts/p31-ci.mjs`**: root `npm run verify` + p31ca build **only if** `andromeda/04_SOFTWARE/p31ca` exists; home-only clone passes without hub build), **P31: wcd33 worker tail**, **P31: Cloudflare wrangler inventory**, **P31: p31ca hub:ci** (registry → about + verify + build). Root: **`npm run release:check`**, **`npm run validate:full`** (extended shell validation + quantum egg + live audits), **`npm run inventory:cf`**. Andromeda **P31 Automation** manual deploy: optional **`p31ca_strict_passport`** requires `cognitive-passport` in the checkout (or fails).

7. **Parallel agents (Andromeda):** if another session is editing **`p31ca`**, read **`andromeda/04_SOFTWARE/p31ca/docs/PARALLEL_AGENT_COORDINATION.md`** before large `index.astro` or ground-truth edits.

8. **Site updates in workflow (velocity):** treat ship cadence as **default**, not an afterthought. **Technical hub (p31ca.org):** lives under **`andromeda/04_SOFTWARE/p31ca/`** — after registry or content changes, **`npm run hub:ci`**, then passport sync if the generator changed, then deploy per **`p31ca/DEPLOY.md`**. **CI:** `p31ca-hub.yml` + `p31-automation` / Pages jobs in Andromeda. **Parallel path — phosphorus31.org:** a **separate** tree, package graph, and deploy story; another agent or operator may own it. **Do not** mix p31ca and `phosphorus31.org/` changes in the same PR unless the operator explicitly wants one coordinated release. See **`P31-ROOT-MAP.md`** (site update tracks).

9. **Handoff for Gemini / Opus (or other model review):** **`docs/README-REVIEW-DOCS.md`** — bundle + CI / worker / CWP supplements + **`docs/MVP-DELIVERABLES-INVENTORY.md`** (tiered MVP / grant bullets). Root **`npm run p31:ci`** = `scripts/p31-ci.mjs` (**`npm run verify`** then p31ca verify+build; in `CI` also root + p31ca **`npm ci`**). **Updated 2026-04-25.** The Triad includes **KwaiPilot** (Node Zero FW execution). WCD Batch 3 (WCDs 26–32) closed; CogPass long-form **edition 5.1** (`CANONICAL-NUMBERING`); P31 Labs, Inc. incorporated Apr 3, 2026; EIN 42-1888158; Stripe direct via **`api.phosphorus31.org`** (HCB unresponsive). See review bundle **§12** for snapshot.

10. **Public surfaces shipped 2026-04-25 (p31ca public release batch):** Four new live pages in `andromeda/04_SOFTWARE/p31ca/public/` — **`planetary-onboard.html`** (five-phase neuro-inclusive onboarding; Void → Anchor → Rooms → Dial → Pact; WebAuthn Phase 5 with Worker fallback stub; archetype routing `?a=child|elder`); **`connect.html`** (Three.js r160 K₄ cage navigator — family tetrahedron + 8 product satellites, EBC footer); **`delta.html`** (Tailwind CDN narrative page — Wye→Delta resilience story, comparison table, CTAs to onboarding and mesh); **`tomography.html`** (Three.js r183 depth companion to k4market — symbol-seeded Larmor ring). **Super-Centaur operator pack:** **`p31-super-centaur-starter.html`** + **`p31-super-centaur-pack.json`**; short redirects **`/centaur`** and **`/super-centaur`**; hub registry id **`super-centaur`**; **`npm run verify:super-centaur-pack`** (p31ca `prebuild`) checks pack shape and **`meshFleet` ↔ `integration-handoff/CWP-30/mesh-bridge.ts`**. **Passkey Worker** (stub, deploy pending): `andromeda/04_SOFTWARE/p31ca/workers/passkey/` — KV `CHALLENGES` namespace (5-min TTL), D1 `p31-passkey-db`, four endpoints (`register-begin/finish`, `auth-begin/finish`). Deploy: `wrangler kv:namespace create CHALLENGES` → `wrangler d1 create p31-passkey-db` → fill IDs in `wrangler.toml` → `wrangler d1 execute p31-passkey-db --file=schema.sql` → `wrangler deploy`. RP_ID: `p31ca.org` (prod), `p31ca.pages.dev` (preview). All four surfaces registered in `ground-truth.json` routes; Three.js pins in `threejs` section for the three that use it. About pages auto-generated via `generate-about-pages.mjs`.

11. **Security suite (p31ca):** `npm run security:check` — SCA (npm audit + suppressions), worker inventory + CORS scan, PQC test gate. Policy: P0 = unsuppressed prod-runtime vulns or broken quantum-core tests (CI fails); P1 = dev-dep CVEs + worker gaps (warns, CI passes). Runbook: `andromeda/04_SOFTWARE/p31ca/docs/SECURITY-RUNBOOK.md`. Edge trust boundary: `docs/EDGE-SECURITY.md`. Suppressions: `security/audit-suppressions.json`. Worker allowlist: `security/worker-allowlist.json`. CI: `.github/workflows/p31-security.yml` (weekly Monday + push to main on security paths). When adding a worker: run `security:workers` and add to allowlist before merging.

12. **Creator economy contract (p31ca):** `ground-truth/creator-economy.json` — `p31.creatorEconomy/1.0.0` schema. Verifier: `npm run verify:economy` (in prebuild; fails CI if fees change without a commit). Public endpoint: `https://p31ca.org/creator-economy.json` (CORS `*`). Version 1.0.0 constraints enforced by CI: platform fee = 0, creator share = 1.0, room access free. To change: edit the JSON, update both `ground-truth/creator-economy.json` and `public/creator-economy.json`, run verifier, commit. 30-day notice policy documented in the contract.

13. **Personal start pages + agents (vision + phases):** **`docs/MESH-MAP-PERSONAL-START-PAGES.md`** — per-member landing + `k4-personal` Durable Object agent (`/agent/:userId/...`); ties to planetary onboard archetypes and cage isolation.

14. **Geodesic campaign contract:** `andromeda/04_SOFTWARE/p31ca/ground-truth/geodesic-campaign.json` — canonical 5-track coach data (Explorer→Scootch→Sticky→Builder→Mesh). TypeScript types + export: `andromeda/04_SOFTWARE/packages/shared/src/geodesic-campaign.ts` (importable as `@p31/shared/geodesic-campaign`). Verifier: `npm run verify:geodesic-campaign` (in p31ca `prebuild`). To change campaign: edit `geodesic-campaign.json` + mirror into the `const CAMPAIGN = { ... }` block in `p31ca/public/geodesic.html` + mirror into `packages/shared/src/geodesic-campaign.ts`. **GeodesicRoom DO v0.2.0** (Phase 1): supports ADD_SHAPE/MOVE_SHAPE/REMOVE_SHAPE/RESET_SHAPES + hello.shapes; 50-shape cap; Maxwell rigidity; worker: `geodesic-room.trimtab-signal.workers.dev`.
