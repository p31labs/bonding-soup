# CWP pointer — Interactive Operator UI

**Full controlled work package:** `andromeda/04_SOFTWARE/p31ca/docs/CONTROLLED-WORK-PACKAGE-INTERACTIVE-OPERATOR-UI.md`  
**ID:** `CWP-P31-UI-2026-01` — **CWP v1.1.0** (complete — hub discovery + local CC + glass summary)  
**Public route (when p31ca is deployed):** `https://p31ca.org/ops/`

If your clone has no `andromeda/` tree, open the same file from the **p31labs/andromeda** monorepo or ask for an export. **Home** repo scripts that support this CWP: `p31-ecosystem.json`, `npm run ecosystem:glass`, `operator:shift-*` (see root `package.json`).
