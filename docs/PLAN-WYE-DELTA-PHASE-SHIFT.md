# PLAN — Wye → Delta phase shift

**Status:** stub. Operator-authored Tier A. Agent prepares the container; operator fills it during morning flow (iPad handwriting → OCR → agent structures).

**Tier:** A (operator voice — not agent-generatable).

**Why this exists:** P31's strategic vision for how decentralized topology replaces centralized infrastructure at civilizational scale. The three golden sentences from the brain dump triage belong here. Naming the future is how futures become real.

---

## 1. What Δ means (topology, not politics)

> TODO — operator voice. The phase shift from Wye (centralized hub-and-spoke) to Delta (K₄ mesh) is a change in **topology**, not ideology. Power was an artifact of star wiring. Decentralization is the wiring change. Politics follows.

Reference frames the operator may want to draw on:
- K₄ as the smallest fully-connected mesh (4 vertices, 6 edges, no central authority).
- Wye = star transformer (centralized rectifier). Delta = closed triangle (every node carries load).
- Fuller's trim tab: turn the small thing, the rudder follows, the ship turns.

## 2. What is nucleated (the K₄ proof)

> TODO — operator voice. P31 today is a seed crystal, not the lattice. List what already exists as a working Δ: the K₄ cage (will / S.J. / W.J. / christyn), edge-only economics (creator-economy.json, no platform fee), the delta language, the ephemeralization spine (one source → derivations → verify), the narrative surface (phos31.org / p31ca.org).

Inventory anchor: `docs/MESH-ARCHITECTURE-CANON.md` (shipped vs next vs doctrine).

## 3. What propagates (every vertex that hosts)

> TODO — operator voice. The lattice grows one vertex at a time. A new vertex is "anyone who hosts a P31 node" — Home Hub on a Pi, a Cloudflare Worker, a Tailscale-reachable terminal. Onboarding is the trim tab. The bottleneck is not topology — it's agency.

Open architectural questions to resolve (do not answer in this doc — note them for later):
- Onboarding as `npm run setup` for non-developers.
- Cage isolation guarantees when N>4.
- Compliance frame for jurisdictions that assume a centralized registrar.

---

## The three golden sentences

> TODO — operator voice. From the brain-dump triage extraction record (see `docs/_archive/brain-dumps/`):
>
> 1. *The geometry decides when you are ready.*
> 2. *Using subconscious triggers for creation, not extraction.*
> 3. *Self-sovereignty in a single device.*
>
> Operator: weave these into §1–§3 above as section seeds. They are the document.

---

## Out of scope

- Quantum-mind-interface speculation (parked: `docs/PARKING-LOT.md`).
- Civilization-scale rollout playbook. P31 at K₄ scale **is** the trim tab; the lattice grows one vertex at a time, by example.
- Adversarial / regulatory red-team. Add when 501(c)(3) lands.

## Authoring contract

- Voice memo → transcribe → agent structures → operator signs.
- Not agent-generated. Tier A.
- Sibling to: `docs/CONCEPT-COGNITIVE-ESCROW.md` (the pattern-naming companion).
- Cross-link from: `docs/MESH-ARCHITECTURE-CANON.md`, `docs/SIC-POVM-K4-ARCHITECTURE.md`, `phosphorus31.org/website/manifesto/`.
