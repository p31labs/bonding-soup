# P31 Office + Foundry (Python)

The **Zenodo scanner** in this tree (`../scripts/zenodo_scan_local.py`) is invoked by **`p31-office zenodo scan`** (`packages/p31-office`).

**Document Foundry** (ingest, manifests, grant scaffold, **`push`**, **`job`** incl. **`list`**, R2 + queue Worker): **`packages/p31-foundry/`** — spec **`docs/P31-DOCUMENT-FOUNDRY.md`**.

**Canonical docs:** **`packages/p31-office/README.md`**, **`packages/p31-foundry/README.md`**, **`P31-ROOT-MAP.md`** §5a.

**Install (from P31 home root):** `npm run office:install` (installs **office + foundry** into `Discovery/.venv`).
