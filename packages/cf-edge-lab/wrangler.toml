# P31 edge lab — `npm run edge-lab:dev` from home root (after npm install in this dir).
# Canonical URL: p31-constants.json mesh.edgeLabWorkerUrl — listed in p31-live-fleet + p31-ecosystem deployables.
name = "cf-edge-lab"
main = "src/index.ts"
compatibility_date = "2026-04-01"

[ai]
binding = "AI"

[observability]
enabled = true
