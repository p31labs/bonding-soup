{
  "name": "@p31-spike/sovereign-geodesic-preview",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Read-only GeodesicRoom GET /state viewer; Vite dev proxy avoids CORS to geodesic-room Worker.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.6.3",
    "vite": "^6.0.1"
  }
}
